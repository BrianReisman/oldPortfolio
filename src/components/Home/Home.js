import React from "react";
import StyledHome from "./StyledHome";

const Home = (props) => {
  return (
    <StyledHome>
      <div className="photoContainer"></div>
    </StyledHome>
  );
};

export default Home;
